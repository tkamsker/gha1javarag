<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE module PUBLIC "-//Google Inc.//DTD Google Web Toolkit 2.8.2//EN"
  "http://gwtproject.org/doctype/2.8.2/gwt-module.dtd">

<module rename-to='app'>
    <!-- Inherit the core Web Toolkit stuff -->
    <inherits name='com.google.gwt.user.User'/>

    <!-- Inherit the default GWT style sheet -->
    <inherits name='com.google.gwt.user.theme.standard.Standard'/>

    <!-- Inherit custom modules -->
    <inherits name='com.example.common.Common'/>
    <inherits name='com.example.shared.Shared'/>

    <!-- Specify the app entry point class -->
    <entry-point class='com.example.client.AppEntryPoint'/>

    <!-- Additional entry points for multi-module setup -->
    <entry-point class='com.example.client.DashboardEntryPoint'/>
    <entry-point class='com.example.client.UserManagementEntryPoint'/>

    <!-- Specify the paths for translatable code -->
    <source path='client'/>
    <source path='shared'/>

    <!-- Public resources -->
    <public path="public" />

    <!-- Compilation configuration -->
    <set-property name="user.agent" value="gecko1_8,safari,ie10"/>
    <set-property name="locale" value="en,de,fr"/>

    <!-- Development mode configuration -->
    <add-linker name="xsiframe"/>
    <set-configuration-property name="devModeRedirectEnabled" value="true"/>

    <!-- Replace certain implementations for specific conditions -->
    <replace-with class="com.example.client.impl.UserPresenterImpl">
        <when-type-is class="com.example.client.UserPresenter"/>
    </replace-with>

    <!-- Collapse all properties to reduce compilation time -->
    <collapse-all-properties />

</module>
