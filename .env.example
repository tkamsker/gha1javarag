# Copy this file to `.env` and adjust values as needed.
#
# Notes:
# - On macOS with Weaviate in Docker and Ollama on host, use http://host.docker.internal:11434
# - On Linux with host-networked Weaviate, use http://127.0.0.1:11434
# - These values are read by both the app and Weaviate modules (via compose).

# -----------------------------
# Source discovery
# -----------------------------
JAVA_SOURCE_DIR=/Users/thomaskamsker/Documents/Atom/vron.one/playground/java/cuco-ui-admin
# JAVA_SOURCE_DIR=/Users/thomaskamsker/Documents/Atom/vron.one/playground/java
JAVA_INCLUDE_GLOBS=**/*.java
JSP_INCLUDE_GLOBS=**/*.jsp,**/*.jspf
GWT_INCLUDE_GLOBS=**/*.gwt.xml,**/*.ui.xml,**/*EntryPoint*.java,**/*Activity*.java,**/*Place*.java,**/*Service*.java,**/*RequestFactory*.java
JS_INCLUDE_GLOBS=**/*.js

# -----------------------------
# Ollama (LLM/Embeddings)
# -----------------------------
# Client-side app base URL for generate/embed calls
OLLAMA_BASE_URL=http://localhost:11434
# Default generation model and embedding model
OLLAMA_MODEL_NAME=llama3.1:8b
OLLAMA_EMBED_MODEL_NAME=nomic-embed-text

# Weaviate module endpoints for Ollama (used by Docker Compose env)
# macOS: http://host.docker.internal:11434
# Linux: http://127.0.0.1:11434
OLLAMA_API_ENDPOINT=http://host.docker.internal:11434
TEXT2VEC_OLLAMA_API_ENDPOINT=http://host.docker.internal:11434
GENERATIVE_OLLAMA_API_ENDPOINT=http://host.docker.internal:11434

# -----------------------------
# Weaviate
# -----------------------------
WEAVIATE_URL=http://localhost:8080
WEAVIATE_GRPC_URL=localhost:50051
# Optional if using auth
WEAVIATE_API_KEY=

# -----------------------------
# App output/logging
# -----------------------------
OUTPUT_DIR=./data/output
BUILD_DIR=./data/build
LOG_LEVEL=INFO

# -----------------------------
# Project metadata
# -----------------------------
DEFAULT_PROJECT_NAME=it17
